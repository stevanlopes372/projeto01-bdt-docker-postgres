# =========================================
# INFRA & CONFIGURAÇÃO (Essencial)
# =========================================
# Carrega variáveis de ambiente do arquivo .env (senhas, chaves de API)
python-dotenv>=1.0.0
# Leitura de arquivos de configuração YAML (muito usado em pipelines)
PyYAML>=6.0.0

# =========================================
# PROCESSAMENTO DE DADOS (Core)
# =========================================
# Manipulação de dados tabular. Travado na v2+ para performance.
pandas>=2.2.0
# Backend de alta performance (C++) para o Pandas ler Parquet e CSV voando.
pyarrow>=15.0.0
# Base numérica (geralmente o pandas já traz, mas vai que...).
numpy>=1.26.0

# =========================================
# QUALIDADE & TESTES (Dev Tools)
# =========================================
# Framework de testes unitários. Sem teste = Sem deploy.
# pytest>=8.0.0
# O "Fiscal" do código: Linter ultra-rápido e organizador de imports.
# ruff>=0.3.0
# O "Formatador": Deixa todo código com a mesma cara (PEP 8).
# black>=24.0.0
# Automação para rodar isso tudo antes de você commitar.
# pre-commit>=3.6.0

# =========================================
# CONECTORES (Descomente se precisar)
# =========================================
# -- Banco de Dados SQL --
# SQLAlchemy>=2.0.0
# psycopg2-binary>=2.9.0  # Driver PostgreSQL (Para dev/local)

# -- Cloud (AWS) --
# boto3>=1.34.0           # SDK da Amazon
# s3fs>=2023.1.0          # Para o Pandas ler direto do S3 (df.read_csv('s3://...'))